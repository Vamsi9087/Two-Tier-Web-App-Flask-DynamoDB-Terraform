# ── Fill in YOUR values here ──────────────────────
# key_pair_name = name of the Key Pair you created in AWS Console
# (EC2 → Key Pairs → Create Key Pair → save the .pem file)

key_pair_name = "my-keypair"   # ← your key pair name
aws_region    = "ap-south-2"  # ← change this
